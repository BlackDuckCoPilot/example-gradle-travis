language: java

notifications:
  email: false
  
jdk:
- oraclejdk8

after_success:
- curl https://copilot-valid.blackducksoftware.com/scripts/init/gradle -o bds_init.gradle
- ./gradlew --init-script bds_init.gradle buildBom -DbdsPluginVersion=5.0.0
- bash <(curl -s https://copilot-valid.blackducksoftware.com/bash/travis) ./build/blackduck/*_bdio.jsonld
